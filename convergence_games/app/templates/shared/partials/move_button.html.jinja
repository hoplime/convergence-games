<button
    class="btn btn-outline btn-accent btn-sm self-end"
    hx-get="/move_menu?leader_id={{ leader_id }}&current_table_allocation_id={{ current_table_allocation_id }}"
    hx-swap="outerHTML"
>
    Move
</button>
