<p class="opacity-50">You're in a group!</p>
<p>People can join your party by entering this code: {{ adventuring_group.name }}</p>
<div class="flex flex-row gap-2">
    <label class="input input-bordered flex items-center gap-2">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16" fill="currentColor" class="h-4 w-4 opacity-70">
            <path
                fill-rule="evenodd"
                d="M14 6a4 4 0 0 1-4.899 3.899l-1.955 1.955a.5.5 0 0 1-.353.146H5v1.5a.5.5 0 0 1-.5.5h-2a.5.5 0 0 1-.5-.5v-2.293a.5.5 0 0 1 .146-.353l3.955-3.955A4 4 0 1 1 14 6Zm-4-2a.75.75 0 0 0 0 1.5.5.5 0 0 1 .5.5.75.75 0 0 0 1.5 0 2 2 0 0 0-2-2Z"
                clip-rule="evenodd"
            />
        </svg>
        <input
            type="text"
            name="host_code"
            id="name"
            class="grow"
            placeholder="Enter a Party Code"
            value="{{ adventuring_group.name }}"
        />
    </label>
    <button
        class="btn btn-primary"
        hx-put="/change_group_name?adventuring_group_id={{ adventuring_group.id }}"
        hx-include="[name='host_code']"
        hx-target="#schedule_contents"
    >
        Update Code
    </button>
</div>
<p>Party Members:</p>
<ul class="flex flex-col gap-2">
    {% for member in adventuring_group.members %}
        <li class="flex flex-row gap-1">
            <span>{{ member.name }}</span>
            {% for i in range(member.golden_d20s) %}
                <div class="mask-d20 mask grid h-6 w-6 place-items-center bg-yellow-400 leading-none text-base-100">
                    <span class="font-bold">20</span>
                </div>
            {% endfor %}
        </li>
    {% endfor %}
</ul>
<button
    class="btn btn-outline btn-accent"
    hx-put="/leave_group?adventuring_group_id={{ adventuring_group.id }}"
    hx-target="#schedule_contents"
    hx-confirm="Are you sure you wish to leave this adventuring party?"
>
    Leave Party
</button>
